# Orca Transition — Stage A Baseline Snapshot

**Recorded:** 2026-09-24T00:13–00:17 +02:00 (machine clock, `date -Is` = `2026-09-24T00:13:42+02:00`)
**Task:** orca-transition.md Task 21 — "Record baseline snapshot (zellij + OpenCode/OMO + Orca userData + perf baseline)"
**Design reference:** `40-oracle-design.md` §8 Stage A
**Nature:** READ-ONLY recording. Nothing was restarted, killed, or reconfigured.

This is the pre-Orca workspace state. Tasks 22 (release candidate) and 25 (soak budgets)
compare against these numbers; every perf command below is recorded verbatim so it can be
re-run identically.

---

## 1. Host / machine state

| Field | Value |
|---|---|
| Timestamp | 2026-09-24T00:13:42+02:00 |
| Uptime | up 3 days, 1:17, 2 users |
| Load average | 18.47, 20.09, 18.56 (1/5/15 min) |
| CPUs | 32 (`nproc`) |
| MemTotal | 31 GiB (32551528 kB) |
| MemAvailable | 4.3 GiB (4531940 kB) |
| SwapTotal / SwapFree | 74 GiB / 36 GiB |

Raw output: [perf-baseline-2026-09-24.txt](file:///home/ezotoff/src/orca/stage-a/perf-baseline-2026-09-24.txt)

---

## 2. Zellij (current primary terminal multiplexer)

| Field | Value |
|---|---|
| Version | `zellij 0.43.1` |
| Config | `~/.config/zellij/config.kdl` (542 lines) |
| Config backup | `~/.config/zellij/config.kdl.bak.manual-2026-03-15T00-26-54_01-00` |
| Layouts dir | `~/.config/zellij/layouts` — **does not exist** (no custom layouts) |
| `copy_command` | `/home/ezotoff/.local/bin/zellij-copy-to-x11-buffers.sh` (custom X11 clipboard script) |
| `copy_on_select` | `true` |
| `keybinds` | `clear-defaults=true` (fully custom keybind set) |

**Keybind modes present** (custom, defaults cleared): `locked`, `pane`, `tab`, `resize`,
`move`, `scroll`, `search`, `session`, `entersearch`, `renametab`, `renamepane`, `tmux`.
`plugins` / `load_plugins` / `web_client` blocks present.

Full config snapshot: [zellij-config-baseline-2026-09-24.kdl](file:///home/ezotoff/src/orca/stage-a/zellij-config-baseline-2026-09-24.kdl)

**`zellij setup --dump` fallback:** NOT available in 0.43.1 — the flag does not exist
(`error: Found argument '--dump' which wasn't expected`; the real flag is `--dump-layout`).
`zellij setup` with no args panics with `ENOTTY` when run without a TTY. The config file
copy above is therefore the authoritative zellij snapshot.

---

## 3. Session / process inventory

### Zellij sessions — `zellij list-sessions -n`
```
opencode [Created 0s ago]
```
Count: **1** (`opencode`).

### tmux sessions — `tmux list-sessions`
```
m1floor: 1 windows (created Wed Sep 23 23:12:23 2026)
```
Count: **1** (`m1floor`).

### OpenCode server processes — `ps -eo pid,lstart,args | grep 'opencode serve'`
```
 930937 Wed Sep 23 23:47:22 2026 /home/ezotoff/.opencode/bin/opencode serve --hostname 127.0.0.1 --port 57521
1452449 Wed Sep 23 12:13:51 2026 /home/ezotoff/.opencode/bin/opencode serve --hostname 127.0.0.1 --port 3021
1472130 Wed Sep 23 12:14:27 2026 /home/ezotoff/.opencode/bin/opencode serve --hostname 127.0.0.1 --port 3030
2457512 Wed Sep 23 15:56:29 2026 opencode serve --port 46946
2477265 Wed Sep 23 15:59:00 2026 /home/ezotoff/.opencode/bin/opencode serve --hostname 127.0.0.1 --port 3040
```
Count: **5** OpenCode server processes (3021 = headless `opencode.service`, 3030 =
interactive, 3040 = bench, plus two ad-hoc instances).

### Relevant systemd user services (read-only `systemctl --user list-units`)
Active: `opencode.service`, `opencode-interactive.service`, `opencode-bench.service`,
`opencode-patch-watcher.service`, `opencode-supervisor.service`, `voice-bridge.service`,
`oc-durable-ez-raider-mud7o3l1-...service`, `rig-text-bench.service`.
Failed (pre-existing, unrelated to this task): `opencode-patch-integrity-check.service`,
`opencode-integrity-triage.service`, `kimi-text-bench{,2,3}.service`.

### Project inventory
`~/src/*` and `~/AI_projects/*` — 40+ project directories present (e.g. `ez-omo-config`,
`ez-omo-dash`, `voice-bridge`, `omo-control-plane`, `omo-tg`, `accounting`, `bim`, `ifc`,
`kraken`, `market_research`, …). Full listing captured at record time; the operator's real
10–14-project/session shape is the Stage B soak target.

---

## 4. OpenCode binary + OMO runtime state

| Field | Value |
|---|---|
| OpenCode binary | `~/.opencode/bin/opencode` → **`1.18.31-p2`** |
| OMO runtime dir | `~/oh-my-openagent-v4.19.2` |
| OMO HEAD | `b63abceb6` — `test(hooks): align agent-name expectations with clean display names` |
| OMO describe | `v4.19.2-patches.1-22-gb63abceb6` |
| OMO working tree | clean (`git status --porcelain` empty) |

### Patch health — `bash ~/.sisyphus/scripts/verify-live-patches.sh`
```
Summary (grep verdicts: omo/opencode-dcp/config layers): 35 total | 19 applied | 3 stale | 0 missing-target | 0 version-drift | 2 acknowledged-drift | 0 schema-violation
Summary (opencode binary provenance): AMBER | binary ae7e1d1c056ade120fec4791837e2be732f3629b3a9f2743e5f40b1988e6d184 | verified | provenance-verified 9 | runtime-verified 2 | runtime-pending 1 | runtime-failed 0 | weak 0
```
Exit code: **1** (3 stale + 2 acknowledged-drift; no missing-target, no version-drift, no
schema-violation). Full output: [patch-health-baseline-2026-09-24.txt](file:///home/ezotoff/src/orca/stage-a/patch-health-baseline-2026-09-24.txt)

---

## 5. Orca userData snapshot

**Orca has NEVER RUN on this machine — no userData directory exists.**

Verified absent (all checked explicitly):
- `~/.config/Orca` (Electron desktop userData; `app.setName('Orca')` → `~/.config/Orca` on Linux) — absent
- `~/.config/orca` — absent
- `~/.local/share/Orca`, `~/.local/share/orca` — absent
- `~/.cache/Orca`, `~/.cache/orca` — absent
- `~/.orca` (orcad Node-host fallback: `$ORCA_USER_DATA` → `$XDG_DATA_HOME/Orca` → `~/.orca`) — absent

Source evidence for the path resolution:
- `src/main/startup/main-process-ready-foundation.ts:101` — `app.setName(identity.appName)`
- `src/main/startup/dev-instance-identity.ts:5` — `const BASE_APP_NAME = 'Orca'`
- `src/main/orcad/orcad-app-paths.ts:22-29` — `resolveUserDataPath()`: `$ORCA_USER_DATA` → `$XDG_DATA_HOME/Orca` → `~/.orca`

**Tarball:** NOT created — there is no source directory to archive. Per task instruction
("create the tarball only if a dir exists"), `orca-userdata-baseline-2026-09-24.tar.gz`
is intentionally absent.

**Restore proof:** N/A — no tarball was written, so there is nothing to extract/diff. The
binding restore-diff proof applies once a userData dir exists; the first Orca launch will
create `~/.config/Orca`, and a fresh snapshot + restore-diff should be taken then.

**Related Orca artifacts present (not userData):**
- `~/Downloads/orca-linux.AppImage` — 197,744,574 bytes, dated 2026-09-18 18:54 (never launched)
- `~/.local/share/orca-toolchain/` — durable Node 24.9.0 + pnpm 12.0.0 toolchain (Task 5)
- `~/src/orca` — fork clone, `operator/main` HEAD `1b4c33f6d3`, clean tree

---

## 6. Baseline perf (memory + idle CPU)

Exact commands and raw output are in
[perf-baseline-2026-09-24.txt](file:///home/ezotoff/src/orca/stage-a/perf-baseline-2026-09-24.txt).
Commands used:

```bash
date -Is
uptime
free -h
nproc
cat /proc/loadavg
ps -eo rss,pcpu,comm --sort=-rss | head -30
systemd-cgtop -b -n 1 | head -20
vmstat 1 3
```

### Top RSS consumers (`ps -eo rss,pcpu,comm --sort=-rss | head -30`)
| RSS (KiB) | %CPU | COMMAND |
|---|---|---|
| 4,143,804 | 56.6 | opencode |
| 2,085,468 | 92.2 | llama-server |
| 2,016,368 | 25.6 | opencode |
| 1,583,328 | 18.7 | bun |
| 1,516,640 | 38.9 | opencode |
| 880,288 | 18.3 | opencode |
| 665,996 | 3.5 | bun |
| 565,376 | 12.7 | opencode |
| 488,812 | 13.1 | opencode |
| 427,532 | 10.6 | opencode |

Aggregate: OpenCode processes dominate RSS; `llama-server` (local model) is the single
largest CPU consumer. `free -h`: 23 GiB used, 1.6 GiB free, 6.3 GiB buff/cache, 6.6 GiB
available; swap 35 GiB used of 74 GiB.

### cgroup overview (`systemd-cgtop -b -n 1 | head -20`)
Root cgroup: 8578 tasks, 24.3 GiB memory. `system.slice`: 1795 tasks, 985.4 MiB.
(user.slice holds the bulk of the memory — the OpenCode/agent fleet.)

### Idle CPU (`vmstat 1 3`)
```
 r  b   swpd   free   buff  cache   si   so    bi    bo   in   cs us sy id wa st
 4 10 40552260 427816 1168904 3818856  169  237  1262   478    5    1  9  2 87  2  0
16  7 40499712 644576 1273864 3887088 63396 24012 447216 49132 119114 199902 20  7 52 21  0
10  6 40600820 618176 1237564 3927728 35580 159820 236952 175880 95574 158385 16  6 59 19  0
```
The machine is **NOT idle** at baseline: load ~19 on 32 cores, sustained swap I/O
(`si`/`so` in the tens of thousands), 16–20% user CPU. This is the operator's normal
working load (agent fleet + local model), not a quiet reference point. Stage B soak
budgets must be measured against this loaded baseline, not an idle machine.

---

## 7. Artifact index (all under `/home/ezotoff/src/orca/stage-a/`)

| Artifact | Purpose |
|---|---|
| `baseline-2026-09-24.md` | This document |
| `zellij-config-baseline-2026-09-24.kdl` | Zellij config snapshot (542 lines) |
| `patch-health-baseline-2026-09-24.txt` | Full `verify-live-patches.sh` output |
| `perf-baseline-2026-09-24.txt` | Raw perf command outputs |
| `orca-userdata-baseline-2026-09-24.tar.gz` | **NOT created** — Orca never ran, no userData dir |

---

## 8. Surprises / notes for later tasks

1. **Orca has never run** — Stage A baseline is genuinely pre-first-run. The first launch
   will create `~/.config/Orca`; snapshot it then.
2. **`zellij setup --dump` does not exist in 0.43.1** — the task's fallback command is
   invalid; the config-file copy is the authoritative snapshot.
3. **Baseline is loaded, not idle** — load ~19/32, heavy swap. Soak thresholds in §8
   ("idle CPU < 20% of one core") must be interpreted against this loaded machine.
4. **3 stale + 2 acknowledged-drift patches** at baseline (exit 1) — pre-existing, not
   caused by this task; recorded so Task 22/25 can detect regressions.
5. **`opencode-patch-integrity-check.service` and `opencode-integrity-triage.service` are
   in `failed` state** at baseline — pre-existing; noted for the record.
